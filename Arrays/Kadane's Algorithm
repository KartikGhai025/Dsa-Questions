 long long maxSubarraySum(int arr[], int n){
        long long mt=INT_MIN, msf=0;
    for(int i=0; i<n;i++){
        msf+=arr[i];
        if(msf>mt) mt= msf;
        if(msf<0) msf=0;
    }
    return mt;
  
    }
